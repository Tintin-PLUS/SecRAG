# 端侧知识库 chunk_size / overlap 技术选型报告

> 决策结论：在 **bge-small + fixed 切分 + Top-K=5 + sqlite-vec** 全部固定的目标配置下，默认采用 **fixed-500-80**，即只把 `chunk_size=500`、`overlap=80` 作为待选参数。100/20、150/30、200/40 均可正常运行，但在本次 34 题数据集上不适合作为默认值：它们没有带来稳定的时延收益，却将块数放大到 2.47～4.84 倍、测试持久化索引放大到 2.38～4.65 倍，同时使 MRR@5 下降 7.1～9.7 个百分点、nDCG@5 下降 8.5～10.6 个百分点、MAP@5 下降 11.1～13.7 个百分点。
>
> 证据边界：现有数据支持的是 **500/80 这一参数组合**，不能单独证明 `overlap=80` 是最优；800/120 在当前短事实题上多项检索指标高于 500/80，因此 500/80 应表述为兼顾排序质量、端侧时延和上下文粒度的保守默认值，而不是所有指标上的全局最优值。

## 1. 决策对象与证据口径

本报告只回答一个问题：在现有端侧知识库中，保持模型、切分策略、Top-K、检索方式和向量存储不变时，为什么默认选择 500/80，而不选择 100/20、150/30 或 200/40。

| 维度 | 固定值 | 是否参与选择 |
|---|---|---|
| Embedding 模型 | bge-small | 否 |
| 切分策略 | fixed，按当前实现的中文字符数计 | 否 |
| Top-K | 5 | 否 |
| 检索方式 | 向量 + 字符 bigram 的 RRF 混合检索 | 否 |
| 目标向量存储 | sqlite-vec | 否 |
| chunk_size / overlap | 100/20、150/30、200/40、300/50、500/80、800/120 | **是，唯一自变量** |

证据来自以下四个文件：

- [20260907 正式 34 题报告](20260907-formal-34q/端侧知识库技术选型与轻量化验证报告-原始34题.md)
- [20260907 聚合数据](20260907-formal-34q/report_data.json)
- [20260908 小切块 34 题报告](20260908-small-chunks-34q/端侧知识库技术选型与轻量化验证报告-原始34题.md)
- [20260908 聚合数据](20260908-small-chunks-34q/report_data.json)

两轮使用相同的数据集 `Q1-24docs-34queries-original`：24 篇文档、34 道原始题，文档总大小 44,173 字节，平均每篇 1,840.5 字节。题目文件均为 `embedding-test/eval/test_queries.json`，大小 4,453 字节，SHA-256 均为 `b40d80c38c8f8af36ede7056c2a4f080445f3949a5403d8b17ba84fe5e3e3508`；24 篇文档的文件名、大小和哈希也逐项一致。

两轮运行环境相同：i9-12900H、20 逻辑线程、15.74 GiB 内存、Windows、CPU-only、平衡电源计划；两轮均为 13 个运行结果、0 条错误、21/21 功能用例通过。

所有质量表统一取 **bge-small + fixed + Top-K=5 + 混合检索**，不使用 K=3/10、其他模型或 structure 切分参与选型。

需要说明一项测试实现边界：chunk 复测的 `run_result.json` 将检索后端标记为 `numpy-cosine+char-bigram-RRF`，sqlite-vec 是在独立随机向量规模测试中验证和选定的。两份聚合数据的 `storage_accuracy` 均显示 `result_overlap_mean=1.0`、`exact_order_match_rate=1.0`，即存储对照中没有观察到结果集合或顺序差异；这为把 chunk 质量排序迁移到目标 sqlite-vec 配置提供了支持，但不是语义 chunk 上的严格证明。块数本身与存储后端无关；表中的 P95/QPS 和持久化索引字节也不能冒充“每个 chunk 组合均由 sqlite-vec 搜索”的严格同轮实测。若需要完全严格的 sqlite-vec A/B，仍需按第 7 节补测。

## 2. 全量固定切分对比：先看绝对值

下表合并两轮 `report_data.json` 中的 fixed 切分结果，均取 Top-K=5。质量指标为混合检索结果。

| 配置 | 块数 | 测试索引字节 | 建库均值 ms | P95 ms | QPS | Hit@5 | MRR@5 | Recall@5 | Precision@5 | nDCG@5 | MAP@5 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 100/20 | 237 | 991,232 | 1,981.73 | 34.00 | 39.00 | 0.971 | 0.836 | 0.799 | 0.324 | 0.753 | 0.669 |
| 150/30 | 162 | 684,032 | 2,019.64 | 29.41 | 43.67 | 0.971 | 0.810 | 0.820 | 0.318 | 0.759 | 0.681 |
| 200/40 | 121 | 507,904 | 1,893.00 | 32.65 | 40.03 | 1.000 | 0.820 | 0.834 | 0.353 | 0.774 | 0.695 |
| 300/50 | 85 | 360,448 | 2,225.63 | 33.15 | 42.34 | 1.000 | 0.839 | 0.859 | 0.365 | 0.807 | 0.736 |
| **500/80** | **49** | **212,992** | **2,321.09** | **26.96** | **41.85** | **1.000** | **0.907** | **0.878** | **0.335** | **0.859** | **0.806** |
| 800/120 | 37 | 163,840 | 2,108.89 | 29.46 | 45.31 | 1.000 | 0.907 | 0.922 | 0.376 | 0.901 | 0.859 |

这张表给出三个直接判断：

1. 100～300 字符区间存在明显的“切得过碎”现象。随着切块放大到 500，排序质量整体改善，并在 500 出现明显跃升；块数和测试索引体积同步下降。
2. 500/80 是第一个同时达到 `Hit@5=1.000`、`MRR@5>0.90`、`nDCG@5>0.85`、`P95<30 ms` 的固定切分点。
3. 800/120 在本数据集的 Recall、Precision、nDCG 和 MAP 上优于 500/80，但它不是 500/80 被选择的反证，而是一个需要单独处理的“更大上下文”候选，详见第 5 节。

## 3. 为什么 100/150/200 不适合作为默认配置

这里的“不可行”是指 **不满足默认技术选型目标**，不是指程序无法运行。三组小切块均完成测试且没有运行错误；问题出在投入产出比。

### 3.1 块数和测试索引成本成倍增加

相对 500/80：

| 配置 | 块数 | 块数增幅 | 测试索引字节 | 索引增幅 | 平均每文档块数 |
|---|---:|---:|---:|---:|---:|
| **500/80** | **49** | 基准 | **212,992** | 基准 | **2.04** |
| 200/40 | 121 | +146.9%，即 2.47 倍 | 507,904 | +138.5%，即 2.38 倍 | 5.04 |
| 150/30 | 162 | +230.6%，即 3.31 倍 | 684,032 | +221.2%，即 3.21 倍 | 6.75 |
| 100/20 | 237 | +383.7%，即 4.84 倍 | 991,232 | +365.4%，即 4.65 倍 | 9.88 |

小切块把同一份 44,173 字节语料展开成更多向量和更多索引记录。当前语料很小，所以测试索引绝对体积仍不到 1 MB；但块数倍数已经出现，扩到更大知识库时会直接传导到嵌入次数、索引写入和更新放大。表内索引字节来自 chunk 测试的 SQLite 持久化文件，不是 sqlite-vec 六组参数的直接体积对比，因此只作趋势证据。

建库时间没有随块数成比例增长，反而是 1.89～2.02 秒，略低于 500/80 的 2.32 秒。这说明当前 24 篇小语料的建库时间受到固定启动开销和运行波动影响，不能用这组小样本的建库均值推翻向量数量增加 2.47～4.84 倍这一容量事实。

### 3.2 默认 K=5 下，排序质量全面下降

下表是相对 500/80 的差值；质量使用百分点，P95/QPS 使用相对百分比。

| 配置 | Hit Δ | MRR Δ | Recall Δ | Precision Δ | nDCG Δ | MAP Δ | P95 Δ | QPS Δ |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 100/20 | -2.9 | -7.1 | -7.9 | -1.2 | -10.6 | -13.7 | +26.1% | -6.8% |
| 150/30 | -2.9 | -9.7 | -5.8 | -1.8 | -10.1 | -12.5 | +9.1% | +4.3% |
| 200/40 | 0.0 | -8.7 | -4.4 | +1.8 | -8.5 | -11.1 | +21.1% | -4.3% |

结论不应只看 Hit：200/40 的 Hit@5 同样为 1.000，但 MRR 低 8.7 个百分点、nDCG 低 8.5 个百分点、MAP 低 11.1 个百分点。这表示“前五名里能找到相关块”不等于“相关块排得足够靠前、整体排序足够好”。对于端侧 RAG，排序靠后会增加后续重排或生成模型从噪声中找证据的负担。

100/20 和 150/30 的 Hit@5=0.971。34 道题中一次命中差异约等于 2.94 个百分点，因此这两组相当于仍有 1 道题在前五名没有相关块，而 500/80 已达到 34/34 命中。

### 3.3 没有形成端侧时延收益

跨轮时延不是严格 A/B：20260907 每个 K 的时延样本为 612，20260908 为 1,020，运行时段也不同。因此时延只应作为趋势证据。

在固定的 K=5 下，100/20、150/30、200/40 的 P95 分别比 500/80 高 26.1%、9.1%、21.1%；三者 QPS 分别变化 -6.8%、+4.3%、-4.3%。只有 150/30 的 QPS 略高，但其 P95 更高，MRR/nDCG/MAP 分别低 9.7/10.1/12.5 个百分点，不能形成足以补偿质量损失的性能收益。

因此，不能用“块小所以检索必然更快”作为选择 100/150/200 的理由。实测中，向量数量增加抵消了单块文本更短的潜在优势。

### 3.4 可能的机制解释，但不冒充实测结论

从“块数大幅上升、MRR/nDCG/MAP 同步下降”的组合现象看，最合理的解释是：100～200 字符把原本相邻的公司名、指标名、时间范围和数值证据拆散，并产生更多同文档兄弟块参与竞争，导致相关信息虽然可能出现在前五名中，却不稳定地排在首位。

这是对指标模式的工程推断。现有聚合数据没有统计“跨边界答案占比”“同文档重复块比例”或“生成答案正确率”，因此不能把该机制写成已经单独验证的因果结论。

## 4. 为什么选择 500/80

### 4.1 500 是从过碎区间进入稳定排序区间的拐点

与相邻的 300/50 在 K=5 下比较，500/80：

- 块数从 85 降到 49，减少 42.4%；
- 测试索引从 360,448 字节降到 212,992 字节，减少 40.9%；
- P95 从 33.15 ms 降到 26.96 ms，减少 18.7%；
- MRR 从 0.839 升到 0.907，增加 6.8 个百分点；
- Recall 从 0.859 升到 0.878，增加 1.9 个百分点；
- nDCG 从 0.807 升到 0.859，增加 5.2 个百分点；
- MAP 从 0.736 升到 0.806，增加 7.0 个百分点。

这不是单一指标的小幅波动，而是排序质量、P95、块数和测试索引体积同时改善。Precision 从 0.365 降到 0.335，是 500/80 相对 300/50 的主要代价，但其 MRR、Recall、nDCG、MAP 均更好，说明总体排序收益更大。

### 4.2 500/80 在默认 K=5 下形成可用的端侧闭环

500/80 + K=5 的实测点为：

- 34/34 题前五名命中，Hit@5=1.000；
- MRR@5=0.907，相关证据通常位于结果前部；
- Recall@5=0.878，nDCG@5=0.859，MAP@5=0.806；
- P95=26.96 ms，QPS=41.85；
- 仅 49 个块，测试持久化索引 212,992 字节。

相对三种小切块，这个点用更少的向量记录获得更高的排序质量；相对 800/120，它保留了更细的上下文粒度，并在 K=5 下具有更低的 P95。

### 4.3 overlap=80 的含义与证据边界

80/500=16%，有效步长为 420 个中文字符。这个重叠比例与已测 fixed 配置的 15%～20% 区间一致：100/20、150/30、200/40 为 20%，300/50 为 16.7%，800/120 为 15%。其工程作用是给固定长度边界保留前后文，同时避免 20% 重叠带来的更高重复率。

但本实验没有保持 `chunk_size=500` 不变并单独测试 overlap=0/40/80/100，因此严格结论只能是：

> **500/80 是已测试组合中的推荐默认值；80 不是经过独立消融证明的唯一最优 overlap。**

这一区分很重要。structure-200/800 的 overlap=0 同时改变了切分策略，不能用于推导 overlap 的单变量因果效果。

## 5. 如何解释 800/120 指标更高，但仍默认 500/80

如果目标函数只包含当前 34 道短事实题的检索质量和索引体积，那么数据更支持 **800/120**：K=5 下它与 500/80 的 MRR 同为 0.907，Recall、Precision、nDCG、MAP 分别高 4.4、4.1、4.2、5.3 个百分点，块数和 DB 还分别少 24.5% 和 23.1%。报告不能回避这一点。

默认仍取 500/80，依据是“保守工程边界”，而不是声称 500/80 在表内击败了 800/120：

- K=5 的 P95 为 26.96 ms，低于 800/120 的 29.46 ms，下降 8.5%；
- 每个候选块上限从 800 字符降到 500 字符。按 K=5 的未去重理论上限，送入后续阶段的原始字符量从 4,000 降到 2,500，减少 37.5%；这是参数上限推算，不是生成链路实测；
- 当前 34 题主要是“公司名＋指标名”的短事实检索，只覆盖 12 篇公司研究文档；另外 12 篇证券规则文档仅作为干扰项，没有对应问题。该分布可能奖励包含更多文本的大块，但不能证明 800 字符在长文、多主题文档和答案生成阶段同样更好；
- 现有报告没有测生成答案正确率、引用定位精度、无关上下文比例或端到端 token 消耗，因此不具备把 800/120 升为默认值所需的下游证据。

所以，当前决策应写成：

- **通用默认：500/80 + K=5。** 以已达到的高排序质量为基础，控制单块上下文上限；
- **检索质量优先候选：800/120。** 在当前短事实题上更强，但需通过端到端问答和长文多主题测试后再晋级；
- **强制小粒度场景备选：200/40。** 它是 100/150/200 中表现最好的固定切分，但不能替代默认 500/80。

## 6. 最终选型矩阵

| 固定配置 | chunk_size / overlap | 结论 |
|---|---:|---|
| bge-small + fixed + Top-K=5 + sqlite-vec | **500 / 80** | **默认**。34/34 命中，MRR 0.907、nDCG 0.859、P95 26.96 ms；相对 100～200 显著减少块数并提高排序质量 |
| bge-small + fixed + Top-K=5 + sqlite-vec | 800 / 120 | 质量优先实验候选。当前数据 nDCG 0.901、MAP 0.859；未完成生成质量和上下文污染验证，不作为默认 |
| bge-small + fixed + Top-K=5 + sqlite-vec | 200 / 40 | 强制短引用片段时的备选。MRR/nDCG/MAP 分别比 500/80 低 8.7/8.5/11.1 个百分点，且块数为 2.47 倍 |
| bge-small + fixed + Top-K=5 + sqlite-vec | 100 / 20 或 150 / 30 | 不推荐。前五名仍有漏检，排序质量下降约 10 个百分点，块数放大超过 3 倍 |

## 7. 结论适用范围与下一步验证

本结论适用于当前 24 篇、44,173 字节中文证券语料和 34 道短事实题，不应外推为所有 RAG 项目的通用参数。34 道题中一道题的变化约等于 2.94 个百分点，且报告没有给出置信区间；两轮运行请求数和时段不同，跨轮 P95/QPS 只能作趋势比较。

如果要把“500/80 是保守默认”升级为“500/80 是经严格实验确认的最优点”，至少需要一次真正接入 sqlite-vec、同轮且随机化顺序的单变量消融。模型固定 bge-small、策略固定 fixed、Top-K 固定 5，只改变以下参数：

1. 固定 `chunk_size=500`，测试 overlap=0/40/80/100，单独确认 80 的贡献；
2. 同一运行会话同时测试 100/20、150/30、200/40、300/50、500/80、800/120，消除跨轮时段和样本数差异；
3. 增加证券规则问题、长文多主题问题和跨段答案，避免短事实题偏置；
4. 除检索指标外，增加答案正确率、引用完整率、无关上下文比例、重复块比例和实际输入 token 数；
5. 报告均值、逐题差异和置信区间，再决定 800/120 是否应替代 500/80。

在这些补充实验完成前，**500/80 是现有质量和块数证据支持、风险表述诚实的默认技术选型；100/150/200 不是不能运行，而是在当前数据上没有足够收益抵偿排序质量和块数膨胀。**
